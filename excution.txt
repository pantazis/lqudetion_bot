from pathlib import Path

base = Path("/mnt/data")

# Runtime update note:
# - support immediate retrain refresh when liquidation event threshold is hit
# - keep rolling window at last 200 valid JSONL rows
# - preserve deterministic state key: (time_left_s, pnl_z_bucket, net_liq_bucket)
# - enforce cooldown to prevent duplicate burst retrains

prompt_md = r"""# Codex Prompt — BTC 5m Heatmap Logs + 200-Line Rolling Window + Auto-Refreshing HTML Dashboard

## Goal

Implement a logging pipeline and a standalone HTML dashboard for a **Polymarket BTC 5m heatmap model**.

The system must:

- write structured log files for the heatmap state observations
- use a **200-line rolling window**
- aggregate win-rate statistics from those logs
- render a **table-style heat map**
- auto-refresh the dashboard every **10 minutes**
- keep implementation simple, explicit, and easy to debug

---

## Main Requirements

### 1) Structured log file
Create a log file for observation rows, for example:

```text
logs/heatmap_observations.jsonl

Each line must be a single JSON object.

Each observation row should represent a state snapshot taken during a 5-minute round.

Required fields per log row
{
  "ts": "2026-04-22T12:00:00Z",
  "market": "BTC_5M",
  "round_id": "2026-04-22T12:00:00Z",
  "time_left_s": 180,
  "pnl_z_bucket": "pos_0_3_to_1",
  "net_liq_bucket": "strong_plus",
  "entry_side": "LONG",
  "entry_price": 84500.25,
  "current_price": 84610.80,
  "final_price": 84720.50,
  "target_label": 1,
  "win": 1,
  "final_pnl_pct": 0.26
}
Field meanings
ts: snapshot timestamp
market: fixed market name
round_id: unique id for a 5-minute round
time_left_s: remaining seconds in the 5m round, bucketed to 20s intervals
pnl_z_bucket: z-score bucket for price distance from round start / anchor
net_liq_bucket: liquidation pressure bucket
entry_side: LONG or SHORT
entry_price: reference price if entered at this snapshot
current_price: current market price at snapshot
final_price: settlement / round close price
target_label: 1 if model target condition is true, else 0
win: 1 if entering at this snapshot would have won, else 0
final_pnl_pct: final hypothetical pnl from this snapshot entry
2) 200-line rolling window

Implement log reading so aggregation uses only the latest 200 lines from the JSONL file.

Important:

if file has fewer than 200 rows, use all rows
if file has more than 200 rows, use only the newest 200 rows
do not load the entire file if avoidable
keep this logic simple and reliable
Preferred implementation

Use a small helper like:

read_last_n_jsonl(path, n=200)

This function should:

read the last n non-empty lines
parse each JSON line safely
skip malformed rows with warning logs
return a list of dict rows
3) Aggregation logic

Aggregate the rolling 200-line window into a heatmap table.

State key

Use:

(time_left_s, pnl_z_bucket, net_liq_bucket)
Aggregated metrics per state

For each state compute:

samples
wins
losses
win_rate
avg_final_pnl_pct
Formulas
losses = samples - wins
win_rate = wins / samples
avg_final_pnl_pct = mean(final_pnl_pct)

Round percentages for display.

4) Heatmap table output shape

The dashboard should show one table per liquidation bucket.

For each net_liq_bucket, render a 2D matrix:

rows = time_left_s
columns = pnl_z_bucket
cell = win rate + samples

Example cell display:

68.4% (38)

Optional small second line:

avg pnl: +0.21%
Required net liquidation buckets

Use this exact order if present:

strong_minus
weak_minus
neutral
weak_plus
strong_plus
Recommended pnl z buckets

Use this exact order if present:

lt_neg_2
neg_2_to_neg_1
neg_1_to_neg_0_3
neg_0_3_to_0_3
pos_0_3_to_1
pos_1_to_2
gt_2
Recommended time-left order
300, 280, 260, 240, 220, 200, 180, 160, 140, 120, 100, 80, 60, 40, 20, 0
5) HTML dashboard page

Create a standalone HTML file, for example:

dashboard/heatmap_dashboard.html

The page should:

be plain HTML + CSS + vanilla JS only
not require any external framework
render clean readable tables
show a title
show last refresh timestamp
show current rolling window size
show one heatmap table per net_liq_bucket
refresh automatically every 10 minutes
Important

Assume the page is regenerated by backend code every 10 minutes, or alternatively use JS auto reload:

<meta http-equiv="refresh" content="600">

Also display somewhere:

Rolling Window: last 200 log lines
Auto Refresh: every 10 minutes
6) Heatmap styling

Use color classes based on win rate:

< 40% = very weak
40% to < 50% = weak
50% to < 60% = neutral
60% to < 70% = good
>= 70% = strong

Keep colors soft and readable.

Cells with low samples should be visually muted:

if samples < 5 add class low-sample
if state missing, show —
7) File structure

Implement with small explicit files like:

src/btc5m/heatmap/
  read_logs.py
  aggregate.py
  render_html.py
  build_dashboard.py

logs/
  heatmap_observations.jsonl

dashboard/
  heatmap_dashboard.html
Responsibilities
read_logs.py → read latest 200 JSONL rows
aggregate.py → build grouped heatmap stats
render_html.py → HTML string generation
build_dashboard.py → main entry point that loads logs and writes dashboard file
8) Error handling

Must handle:

missing log file
empty log file
malformed JSON lines
missing fields
divide-by-zero
unknown buckets

Dashboard should still render with a human-readable message instead of crashing.

9) Sample implementation checklist

Create a markdown checklist in comments or docstring and follow it:

## Implementation Checklist
- [ ] Create JSONL log reader for last 200 lines
- [ ] Parse rows safely
- [ ] Normalize bucket ordering
- [ ] Aggregate samples / wins / avg pnl
- [ ] Generate one HTML table per liquidation bucket
- [ ] Add color classes by win rate
- [ ] Add low-sample cell styling
- [ ] Add auto-refresh every 10 minutes
- [ ] Write final HTML file to dashboard/heatmap_dashboard.html
- [ ] Add clear debug logs
10) Deliverables

Generate code for:

read_logs.py
aggregate.py
render_html.py
build_dashboard.py
example heatmap_dashboard.html

Keep code:

production-clean
heavily readable
no unnecessary abstractions
easy to modify later
11) Extra notes
Prefer deterministic ordering of rows/columns
Use explicit helper functions
Keep functions short
Add comments where logic is non-obvious
Do not introduce frameworks
Do not over-engineer
Final instruction to Codex

Implement the full feature end-to-end exactly as described above.

Return complete file contents for all created files.
"""

html_template = r"""<!DOCTYPE html>

<html lang="en"> <head> <meta charset="UTF-8" /> <meta http-equiv="refresh" content="600" /> <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <title>BTC 5m Heatmap Dashboard</title> <style> :root { --bg: #0f172a; --panel: #111827; --panel-2: #1f2937; --border: #334155; --text: #e5e7eb; --muted: #94a3b8; --weakest: #4b1d1d; --weak: #5b3340; --mid: #3f3f46; --good: #1f4d3b; --strong: #14532d; --low-sample: #374151; }
* { box-sizing: border-box; }
body {
  margin: 0;
  padding: 24px;
  font-family: Arial, Helvetica, sans-serif;
  background: var(--bg);
  color: var(--text);
}

h1, h2, h3, p { margin-top: 0; }

.topbar {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 16px 18px;
  margin-bottom: 20px;
}

.meta {
  display: flex;
  gap: 18px;
  flex-wrap: wrap;
  color: var(--muted);
  font-size: 14px;
}

.bucket-section {
  margin-bottom: 26px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 16px;
}

.table-wrap {
  overflow-x: auto;
}

table {
  width: 100%;
  border-collapse: collapse;
  min-width: 900px;
  margin-top: 10px;
}

th, td {
  border: 1px solid var(--border);
  padding: 10px;
  text-align: center;
  vertical-align: middle;
}

th {
  background: var(--panel-2);
  font-size: 13px;
}

td {
  font-size: 13px;
  line-height: 1.35;
}

td .sub {
  display: block;
  margin-top: 4px;
  font-size: 11px;
  color: #cbd5e1;
}

.rate-very-weak { background: var(--weakest); }
.rate-weak      { background: var(--weak); }
.rate-mid       { background: var(--mid); }
.rate-good      { background: var(--good); }
.rate-strong    { background: var(--strong); }
.low-sample     { outline: 2px dashed #9ca3af; }
.missing        { color: var(--muted); background: #0b1220; }

.legend {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 12px;
  font-size: 12px;
  color: var(--muted);
}

.legend span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.swatch {
  width: 14px;
  height: 14px;
  border-radius: 4px;
  border: 1px solid var(--border);
  display: inline-block;
}

.msg {
  color: var(--muted);
  padding: 14px;
  border: 1px dashed var(--border);
  border-radius: 12px;
  background: rgba(255,255,255,0.02);
}
</style> </head> <body> <div class="topbar"> <h1>BTC 5m Heatmap Dashboard</h1> <div class="meta"> <div><strong>Rolling Window:</strong> last 200 log lines</div> <div><strong>Auto Refresh:</strong> every 10 minutes</div> <div><strong>Last Refresh:</strong> <span id="refreshTime">—</span></div> <div><strong>Rows Loaded:</strong> <span id="rowsLoaded">0</span></div> </div> <div class="legend"> <span><i class="swatch" style="background:#4b1d1d;"></i> &lt; 40%</span> <span><i class="swatch" style="background:#5b3340;"></i> 40–49.99%</span> <span><i class="swatch" style="background:#3f3f46;"></i> 50–59.99%</span> <span><i class="swatch" style="background:#1f4d3b;"></i> 60–69.99%</span> <span><i class="swatch" style="background:#14532d;"></i> ≥ 70%</span> <span><i class="swatch" style="background:#374151;"></i> low sample outline if n &lt; 5</span> </div> </div> <div id="app"></div> <script> const TIME_BUCKETS = [300,280,260,240,220,200,180,160,140,120,100,80,60,40,20,0]; const PNL_BUCKETS = [ "lt_neg_2", "neg_2_to_neg_1", "neg_1_to_neg_0_3", "neg_0_3_to_0_3", "pos_0_3_to_1", "pos_1_to_2", "gt_2" ]; const LIQ_BUCKETS = [ "strong_minus", "weak_minus", "neutral", "weak_plus", "strong_plus" ]; // Replace this payload with backend-generated JSON if desired. const dashboardData = { generated_at: new Date().toISOString(), rows_loaded: 0, tables: {} }; function rateClass(winRate) { if (winRate == null) return "missing"; if (winRate < 0.40) return "rate-very-weak"; if (winRate < 0.50) return "rate-weak"; if (winRate < 0.60) return "rate-mid"; if (winRate < 0.70) return "rate-good"; return "rate-strong"; } function fmtPct(value, digits = 1) { if (value == null || Number.isNaN(value)) return "—"; return (value * 100).toFixed(digits) + "%"; } function humanBucketName(value) { return value.replaceAll("_", " "); } function renderTable(bucketName, tableData) { const section = document.createElement("section"); section.className = "bucket-section"; const title = document.createElement("h2"); title.textContent = "Liquidation Bucket: " + humanBucketName(bucketName); section.appendChild(title); const wrap = document.createElement("div"); wrap.className = "table-wrap"; const table = document.createElement("table"); const thead = document.createElement("thead"); const headRow = document.createElement("tr"); const corner = document.createElement("th"); corner.textContent = "Time Left \\ PnL Z-Bucket"; headRow.appendChild(corner); for (const pnlBucket of PNL_BUCKETS) { const th = document.createElement("th"); th.textContent = humanBucketName(pnlBucket); headRow.appendChild(th); } thead.appendChild(headRow); table.appendChild(thead); const tbody = document.createElement("tbody"); for (const timeLeft of TIME_BUCKETS) { const tr = document.createElement("tr"); const th = document.createElement("th"); th.textContent = String(timeLeft); tr.appendChild(th); for (const pnlBucket of PNL_BUCKETS) { const key = `${timeLeft}__${pnlBucket}`; const cellData = tableData?.[key] || null; const td = document.createElement("td"); if (!cellData) { td.className = "missing"; td.textContent = "—"; } else { td.className = rateClass(cellData.win_rate); if ((cellData.samples || 0) < 5) { td.classList.add("low-sample"); } const main = document.createElement("div"); main.textContent = `${fmtPct(cellData.win_rate)} (${cellData.samples})`; const sub = document.createElement("span"); sub.className = "sub"; sub.textContent = `avg pnl: ${fmtPct(cellData.avg_final_pnl_pct)}`; td.appendChild(main); td.appendChild(sub); } tr.appendChild(td); } tbody.appendChild(tr); } table.appendChild(tbody); wrap.appendChild(table); section.appendChild(wrap); return section; } function renderApp() { document.getElementById("refreshTime").textContent = dashboardData.generated_at || "—"; document.getElementById("rowsLoaded").textContent = String(dashboardData.rows_loaded || 0); const app = document.getElementById("app"); app.innerHTML = ""; const hasAny = LIQ_BUCKETS.some(bucket => dashboardData.tables && dashboardData.tables[bucket]); if (!hasAny) { const empty = document.createElement("div"); empty.className = "msg"; empty.textContent = "No heatmap data available yet. Generate aggregated tables from the latest 200 JSONL log rows and inject them into dashboardData."; app.appendChild(empty); return; } for (const liqBucket of LIQ_BUCKETS) { if (dashboardData.tables?.[liqBucket]) { app.appendChild(renderTable(liqBucket, dashboardData.tables[liqBucket])); } } } renderApp(); </script> </body> </html> """

(base / "codex_prompt_heatmap_logs_dashboard.md").write_text(prompt_md, encoding="utf-8")
(base / "heatmap_dashboard_template.html").write_text(html_template, encoding="utf-8")

print("Created files:")
print(base / "codex_prompt_heatmap_logs_dashboard.md")
print(base / "heatmap_dashboard_template.html")

I hit a file-generation hiccup, so I’m regenerating both files now and will give you working download links. 

Analyzed
from pathlib import Path

base = Path("/mnt/data")

prompt_md = """# Codex Prompt — BTC 5m Heatmap Logs + 200-Line Rolling Window + Auto-Refreshing HTML Dashboard

## Goal

Implement a logging pipeline and a standalone HTML dashboard for a **Polymarket BTC 5m heatmap model**.

The system must:

- write structured log files for the heatmap state observations
- use a **200-line rolling window**
- aggregate win-rate statistics from those logs
- render a **table-style heat map**
- auto-refresh the dashboard every **10 minutes**
- keep implementation simple, explicit, and easy to debug

---

## Main Requirements

### 1) Structured log file
Create a log file for observation rows, for example:

```text
logs/heatmap_observations.jsonl

Each line must be a single JSON object.

Each observation row should represent a state snapshot taken during a 5-minute round.

Required fields per log row
{
  "ts": "2026-04-22T12:00:00Z",
  "market": "BTC_5M",
  "round_id": "2026-04-22T12:00:00Z",
  "time_left_s": 180,
  "pnl_z_bucket": "pos_0_3_to_1",
  "net_liq_bucket": "strong_plus",
  "entry_side": "LONG",
  "entry_price": 84500.25,
  "current_price": 84610.80,
  "final_price": 84720.50,
  "target_label": 1,
  "win": 1,
  "final_pnl_pct": 0.26
}
Field meanings
ts: snapshot timestamp
market: fixed market name
round_id: unique id for a 5-minute round
time_left_s: remaining seconds in the 5m round, bucketed to 20s intervals
pnl_z_bucket: z-score bucket for price distance from round start / anchor
net_liq_bucket: liquidation pressure bucket
entry_side: LONG or SHORT
entry_price: reference price if entered at this snapshot
current_price: current market price at snapshot
final_price: settlement / round close price
target_label: 1 if model target condition is true, else 0
win: 1 if entering at this snapshot would have won, else 0
final_pnl_pct: final hypothetical pnl from this snapshot entry
2) 200-line rolling window

Implement log reading so aggregation uses only the latest 200 lines from the JSONL file.

Important:

if file has fewer than 200 rows, use all rows
if file has more than 200 rows, use only the newest 200 rows
do not load the entire file if avoidable
keep this logic simple and reliable
Preferred implementation

Use a small helper like:

read_last_n_jsonl(path, n=200)

This function should:

read the last n non-empty lines
parse each JSON line safely
skip malformed rows with warning logs
return a list of dict rows
3) Aggregation logic

Aggregate the rolling 200-line window into a heatmap table.

State key

Use:

(time_left_s, pnl_z_bucket, net_liq_bucket)
Aggregated metrics per state

For each state compute:

samples
wins
losses
win_rate
avg_final_pnl_pct
Formulas
losses = samples - wins
win_rate = wins / samples
avg_final_pnl_pct = mean(final_pnl_pct)

Round percentages for display.

4) Heatmap table output shape

The dashboard should show one table per liquidation bucket.

For each net_liq_bucket, render a 2D matrix:

rows = time_left_s
columns = pnl_z_bucket
cell = win rate + samples

Example cell display:

68.4% (38)

Optional small second line:

avg pnl: +0.21%
Required net liquidation buckets

Use this exact order if present:

strong_minus
weak_minus
neutral
weak_plus
strong_plus
Recommended pnl z buckets

Use this exact order if present:

lt_neg_2
neg_2_to_neg_1
neg_1_to_neg_0_3
neg_0_3_to_0_3
pos_0_3_to_1
pos_1_to_2
gt_2
Recommended time-left order
300, 280, 260, 240, 220, 200, 180, 160, 140, 120, 100, 80, 60, 40, 20, 0
5) HTML dashboard page

Create a standalone HTML file, for example:

dashboard/heatmap_dashboard.html

The page should:

be plain HTML + CSS + vanilla JS only
not require any external framework
render clean readable tables
show a title
show last refresh timestamp
show current rolling window size
show one heatmap table per net_liq_bucket
refresh automatically every 10 minutes
Important

Assume the page is regenerated by backend code every 10 minutes, or alternatively use JS auto reload:

<meta http-equiv="refresh" content="600">

Also display somewhere:

Rolling Window: last 200 log lines
Auto Refresh: every 10 minutes
6) Heatmap styling

Use color classes based on win rate:

< 40% = very weak
40% to < 50% = weak
50% to < 60% = neutral
60% to < 70% = good
>= 70% = strong

Keep colors soft and readable.

Cells with low samples should be visually muted:

if samples < 5 add class low-sample
if state missing, show —
7) File structure

Implement with small explicit files like:

src/btc5m/heatmap/
  read_logs.py
  aggregate.py
  render_html.py
  build_dashboard.py

logs/
  heatmap_observations.jsonl

dashboard/
  heatmap_dashboard.html
Responsibilities
read_logs.py → read latest 200 JSONL rows
aggregate.py → build grouped heatmap stats
render_html.py → HTML string generation
build_dashboard.py → main entry point that loads logs and writes dashboard file
8) Error handling

Must handle:

missing log file
empty log file
malformed JSON lines
missing fields
divide-by-zero
unknown buckets

Dashboard should still render with a human-readable message instead of crashing.

9) Sample implementation checklist

Create a markdown checklist in comments or docstring and follow it:

## Implementation Checklist
- [ ] Create JSONL log reader for last 200 lines
- [ ] Parse rows safely
- [ ] Normalize bucket ordering
- [ ] Aggregate samples / wins / avg pnl
- [ ] Generate one HTML table per liquidation bucket
- [ ] Add color classes by win rate
- [ ] Add low-sample cell styling
- [ ] Add auto-refresh every 10 minutes
- [ ] Write final HTML file to dashboard/heatmap_dashboard.html
- [ ] Add clear debug logs
10) Deliverables

Generate code for:

read_logs.py
aggregate.py
render_html.py
build_dashboard.py
example heatmap_dashboard.html

Keep code:

production-clean
heavily readable
no unnecessary abstractions
easy to modify later
11) Extra notes
Prefer deterministic ordering of rows/columns
Use explicit helper functions
Keep functions short
Add comments where logic is non-obvious
Do not introduce frameworks
Do not over-engineer
Final instruction to Codex

Implement the full feature end-to-end exactly as described above.

Return complete file contents for all created files.
"""

html_template = """<!DOCTYPE html>

<html lang="en"> <head> <meta charset="UTF-8" /> <meta http-equiv="refresh" content="600" /> <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <title>BTC 5m Heatmap Dashboard</title> <style> :root { --bg: #0f172a; --panel: #111827; --panel-2: #1f2937; --border: #334155; --text: #e5e7eb; --muted: #94a3b8; --weakest: #4b1d1d; --weak: #5b3340; --mid: #3f3f46; --good: #1f4d3b; --strong: #14532d; }
* { box-sizing: border-box; }
body {
  margin: 0;
  padding: 24px;
  font-family: Arial, Helvetica, sans-serif;
  background: var(--bg);
  color: var(--text);
}

.topbar {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 16px 18px;
  margin-bottom: 20px;
}

.meta {
  display: flex;
  gap: 18px;
  flex-wrap: wrap;
  color: var(--muted);
  font-size: 14px;
}

.bucket-section {
  margin-bottom: 26px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 16px;
}

.table-wrap { overflow-x: auto; }

table {
  width: 100%;
  border-collapse: collapse;
  min-width: 900px;
  margin-top: 10px;
}

th, td {
  border: 1px solid var(--border);
  padding: 10px;
  text-align: center;
  vertical-align: middle;
}

th { background: var(--panel-2); font-size: 13px; }
td { font-size: 13px; line-height: 1.35; }

td .sub {
  display: block;
  margin-top: 4px;
  font-size: 11px;
  color: #cbd5e1;
}

.rate-very-weak { background: var(--weakest); }
.rate-weak      { background: var(--weak); }
.rate-mid       { background: var(--mid); }
.rate-good      { background: var(--good); }
.rate-strong    { background: var(--strong); }
.low-sample     { outline: 2px dashed #9ca3af; }
.missing        { color: var(--muted); background: #0b1220; }

.legend {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 12px;
  font-size: 12px;
  color: var(--muted);
}

.legend span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.swatch {
  width: 14px;
  height: 14px;
  border-radius: 4px;
  border: 1px solid var(--border);
  display: inline-block;
}

.msg {
  color: var(--muted);
  padding: 14px;
  border: 1px dashed var(--border);
  border-radius: 12px;
  background: rgba(255,255,255,0.02);
}
</style> </head> <body> <div class="topbar"> <h1>BTC 5m Heatmap Dashboard</h1> <div class="meta"> <div><strong>Rolling Window:</strong> last 200 log lines</div> <div><strong>Auto Refresh:</strong> every 10 minutes</div> <div><strong>Last Refresh:</strong> <span id="refreshTime">—</span></div> <div><strong>Rows Loaded:</strong> <span id="rowsLoaded">0</span></div> </div> <div class="legend"> <span><i class="swatch" style="background:#4b1d1d;"></i> &lt; 40%</span> <span><i class="swatch" style="background:#5b3340;"></i> 40–49.99%</span> <span><i class="swatch" style="background:#3f3f46;"></i> 50–59.99%</span> <span><i class="swatch" style="background:#1f4d3b;"></i> 60–69.99%</span> <span><i class="swatch" style="background:#14532d;"></i> ≥ 70%</span> <span><i class="swatch" style="background:#374151;"></i> low sample outline if n &lt; 5</span> </div> </div> <div id="app"></div> <script> const TIME_BUCKETS = [300,280,260,240,220,200,180,160,140,120,100,80,60,40,20,0]; const PNL_BUCKETS = [ "lt_neg_2", "neg_2_to_neg_1", "neg_1_to_neg_0_3", "neg_0_3_to_0_3", "pos_0_3_to_1", "pos_1_to_2", "gt_2" ]; const LIQ_BUCKETS = [ "strong_minus", "weak_minus", "neutral", "weak_plus", "strong_plus" ]; const dashboardData = { generated_at: new Date().toISOString(), rows_loaded: 0, tables: {} }; function rateClass(winRate) { if (winRate == null) return "missing"; if (winRate < 0.40) return "rate-very-weak"; if (winRate < 0.50) return "rate-weak"; if (winRate < 0.60) return "rate-mid"; if (winRate < 0.70) return "rate-good"; return "rate-strong"; } function fmtPct(value, digits = 1) { if (value == null || Number.isNaN(value)) return "—"; return (value * 100).toFixed(digits) + "%"; } function humanBucketName(value) { return value.replaceAll("_", " "); } function renderTable(bucketName, tableData) { const section = document.createElement("section"); section.className = "bucket-section"; const title = document.createElement("h2"); title.textContent = "Liquidation Bucket: " + humanBucketName(bucketName); section.appendChild(title); const wrap = document.createElement("div"); wrap.className = "table-wrap"; const table = document.createElement("table"); const thead = document.createElement("thead"); const headRow = document.createElement("tr"); const corner = document.createElement("th"); corner.textContent = "Time Left \\\\ PnL Z-Bucket"; headRow.appendChild(corner); for (const pnlBucket of PNL_BUCKETS) { const th = document.createElement("th"); th.textContent = humanBucketName(pnlBucket); headRow.appendChild(th); } thead.appendChild(headRow); table.appendChild(thead); const tbody = document.createElement("tbody"); for (const timeLeft of TIME_BUCKETS) { const tr = document.createElement("tr"); const th = document.createElement("th"); th.textContent = String(timeLeft); tr.appendChild(th); for (const pnlBucket of PNL_BUCKETS) { const key = `${timeLeft}__${pnlBucket}`; const cellData = tableData?.[key] || null; const td = document.createElement("td"); if (!cellData) { td.className = "missing"; td.textContent = "—"; } else { td.className = rateClass(cellData.win_rate); if ((cellData.samples || 0) < 5) td.classList.add("low-sample"); const main = document.createElement("div"); main.textContent = `${fmtPct(cellData.win_rate)} (${cellData.samples})`; const sub = document.createElement("span"); sub.className = "sub"; sub.textContent = `avg pnl: ${fmtPct(cellData.avg_final_pnl_pct)}`; td.appendChild(main); td.appendChild(sub); } tr.appendChild(td); } tbody.appendChild(tr); } table.appendChild(tbody); wrap.appendChild(table); section.appendChild(wrap); return section; } function renderApp() { document.getElementById("refreshTime").textContent = dashboardData.generated_at || "—"; document.getElementById("rowsLoaded").textContent = String(dashboardData.rows_loaded || 0); const app = document.getElementById("app"); app.innerHTML = ""; const hasAny = LIQ_BUCKETS.some(bucket => dashboardData.tables && dashboardData.tables[bucket]); if (!hasAny) { const empty = document.createElement("div"); empty.className = "msg"; empty.textContent = "No heatmap data available yet. Inject aggregated last-200-log-line data into dashboardData.tables."; app.appendChild(empty); return; } for (const liqBucket of LIQ_BUCKETS) { if (dashboardData.tables?.[liqBucket]) { app.appendChild(renderTable(liqBucket, dashboardData.tables[liqBucket])); } } } renderApp(); </script> </body> </html> """

(base / "codex_prompt_heatmap_logs_dashboard.md").write_text(prompt_md, encoding="utf-8")
(base / "heatmap_dashboard_template.html").write_text(html_template, encoding="utf-8")

print("Created:")
print("/mnt/data/codex_prompt_heatmap_logs_dashboard.md")
print("/mnt/data/heatmap_dashboard_template.html")

STDOUT/STDERR
Created:
/mnt/data/codex_prompt_heatmap_logs_dashboard.md
/mnt/data/heatmap_dashboard_template.html


Created both files:

Codex prompt (.md)
HTML heatmap template (.html)

The prompt tells Codex to:

log JSONL rows
use the last 200 lines as a rolling window
aggregate win rate by time_left × pnl_z_bucket × net_liq_bucket
generate a table-style heatmap page
refresh every 10 minutes

The HTML file is a ready template with:

table heatmap layout
color classes by win rate
low-sample highlighting
auto-refresh every 600 seconds

If you want, next I can make a full Python backend version too, with:
read_logs.py, aggregate.py, render_html.py, and build_dashboard.py. 